{
  "name": "vibe-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "echo 'Run client and server separately: cd client && pnpm dev ; cd ../server && pnpm dev'"
  }
}
